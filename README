To-Do List Application:

This is a simple command-line To-Do List application written in Python. It allows users to manage their tasks by adding, viewing, marking as complete, and deleting tasks.

Features
Add a task: Users can add tasks to their To-Do List by providing a title.
View tasks: Users can view their existing tasks along with their statuses (Incomplete or Complete).
Mark a task as complete: Users can mark a task as complete by specifying its index.
Delete a task: Users can delete a task from their To-Do List by specifying its index.
Quit: Users can exit the application.